{
  "name": "email-reader",
  "version": "1.0.0",
  "description": "Access emails from an inbox using the Imap client",
  "main": "index.js",
  "scripts": {
    "start": "NODE_TLS_REJECT_UNAUTHORIZED='0' node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "dotenv": "^8.2.0",
    "emailjs-imap-client": "^3.1.0"
  }
}
